# ── dependencies ──
node_modules/
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# ── testing ──
/coverage/

# ── next.js ──
/.next/
/out/
/build/

# ── misc ──
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# ── env files (commit only the template) ──
.env*
!.env.example

# ── local SQLite database (Neon in prod) ──
/db/*.db
/db/*.db-journal

# ── Flutter build output ──
flutter_app/build/
flutter_app/.dart_tool/
flutter_app/.flutter-plugins
flutter_app/.flutter-plugins-dependencies

# ── Vercel ──
.vercel/

# ── TypeScript ──
*.tsbuildinfo
next-env.d.ts

# ── logs ──
*.log
dev.log
dev.out.log
server.log

# ── delivery archive (bundled separately for download) ──
public/tradeseekho-source.tar.gz

# ── sandbox-internal (NOT part of TradeSeekho) ──
/skills/
/mini-services/
/upload/
/examples/
/tests/
/download/
/worklog.md
/local-*
/.claude/
/.z-ai-config/
/.zscripts/
/Caddyfile
/prompt
